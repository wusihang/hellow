#include <QApplication>
#include<QtWidgets>
#include "user-defined-component/iconEditor/iconeditor.h"
int main(int argc, char *argv[])
{
    QApplication app(argc,argv);
    IconEditor icon;
    icon.setZoomFactor(2);
    icon.show();
    return app.exec();
}
